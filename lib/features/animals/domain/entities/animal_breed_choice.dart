/// Representa una opción de raza disponible para selección en el formulario.
class AnimalBreedChoice {
  final String value;
  final String label;

  const AnimalBreedChoice({
    required this.value,
    required this.label,
  });
}
