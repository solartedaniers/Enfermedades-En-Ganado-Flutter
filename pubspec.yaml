name: agrovet_ai
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.12.0
  provider: ^6.1.2
  fluttertoast: ^9.0.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2   # 👈 Aquí agregamos la librería

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true